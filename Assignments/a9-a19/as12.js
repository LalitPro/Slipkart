// Assignment 12
let a = "Dar tu bhag ja bhai aaya dikhta nhi kya ILLUMINTAI";
for (let i = 0; i < 10000000000000000000000000000000000000000000000; i++) {
  console.log(a, a, a, a, a, a, a, a, a, a, a, a);
}

/*

  //
   // User se tab tak input lena hai jab tak vo cancel nahi 
    //kar dete hain phir sabhi ka average dikhana hai
    //

// Ek array bana lete hain jismen user dyara input kiye gaye numbers ko store karenge.

let userNumbs = [];

// User se input lene ke liye
let input = +prompt("number likho yaar kuchh please Avg nikalne ke liye?");

userNumbs.push(input.valueOf);

while (true) {
  let userInput = +prompt("number likho bhai?");
  if (userInput === 0) {
    break;
  }
  userNumbs.push(userInput.valueOf);
}
// ek variable banate hain jismen total karte jayenge

let total = 0;

for (let i = 0; i < userNumbs.length; i++) {
  total += userNumbs[i];
}

// avg naam ka variable banakar usmen avg store karenge

let avg = total / userNumbs.length;

// Website par avg show karenge

document.write(`
  <p>The Average of ${userNumbs} is</p>
  <h1>${avg}</h1>
  `);

  */
